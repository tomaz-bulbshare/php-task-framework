# Description
Basic Larevel 9 framework with PHP 8.

# Requirements
* docker
* docker-compose

# Usage

## install
1. docker-compose run myapp bash
2. execute: composer install
3. exit container

## start container
docker-compose up

## stop containes
docker-compose down
